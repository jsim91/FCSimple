% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcs_plot_meta_regions.R
\name{fcs_plot_meta_regions}
\alias{fcs_plot_meta_regions}
\title{Visualize Meta-Region Integrity on a 2D Embedding}
\usage{
fcs_plot_meta_regions(
  fcs_join_obj,
  reduction,
  metric = "retention",
  highlight_top = 20,
  point_alpha = 0.2,
  outdir = getwd(),
  figure_width = 10,
  figure_height = 10,
  plotting_device = "pdf",
  add_timestamp = TRUE,
  return_plot = FALSE
)
}
\arguments{
\item{fcs_join_obj}{A list produced by \code{FCSimple::fcs_embedding_neighbors()} and
\code{FCSimple::fcs_meta_regions()}.}

\item{reduction}{Character; the 2D reduction to plot (e.g. \code{"umap"}).
Case-insensitive.}

\item{metric}{Character; per-meta-region integrity metric to highlight:
\code{"retention"} (default) or \code{"mixing"}.}

\item{highlight_top}{Integer; number of worst meta-regions (lowest retention or highest mixing)
to highlight as enlarged markers.  Default \code{20}.}

\item{point_alpha}{Numeric; point transparency for background cells.  Default \code{0.2}.}

\item{outdir}{Character; directory to save the plot when \code{return_plot = FALSE}.
Default \code{getwd()}.}

\item{figure_width, }{figure_height
Numeric; plot dimensions in inches.  Default \code{10}.}

\item{plotting_device}{Character; \code{"pdf"} or \code{"png"}.  Default \code{"pdf"}.}

\item{add_timestamp}{Logical; append a timestamp to the filename.  Default \code{TRUE}.}

\item{return_plot}{Logical; return the ggplot object instead of writing to file.
Default \code{FALSE}.}
}
\value{
If \code{return_plot = TRUE}, a ggplot object; otherwise the plot is
written to \code{outdir} and \code{NULL} is returned invisibly.
}
\description{
Colours a 2D reduction embedding by each cell's meta-region, with optional
overlays highlighting meta-regions whose composition is poorly preserved
(low retention / high mixing) after projection from high dimensions.
}
\details{
Background cells are coloured by meta-region id (categorical palette).
Then the \code{highlight_top} worst meta-regions according to
\code{metric} are drawn as enlarged black-outlined points along with their
meta-region ids, so the viewer can locate where high-dimensional coherent
patches break apart in the low-dimensional layout.
}
\examples{
\dontrun{
  obj <- fcs_meta_regions(obj, resolution_parameter = 5, n_hops = 2)
  fcs_plot_meta_regions(obj, "umap", metric = "retention")
}

}
\seealso{
FCSimple::fcs_meta_regions, FCSimple::fcs_embedding_neighbors
}
