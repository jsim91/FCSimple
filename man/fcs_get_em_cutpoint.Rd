% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcs_gating.R
\name{fcs_get_em_cutpoint}
\alias{fcs_get_em_cutpoint}
\title{EM‐Based Cutpoint Estimation}
\usage{
fcs_get_em_cutpoint(
  x,
  em_method = c("mclust", "mix"),
  general_method = c("gmm", "flex", "mean"),
  post.thresh = 0.5,
  prom_tol = 0.05,
  flex_tail = 0.75,
  bw_adjust = 2,
  n_grid = 512,
  curvature_eps = 0.01,
  return.model = TRUE
)
}
\arguments{
\item{x}{Numeric vector of observations.}

\item{em_method}{Character; “mclust” (default) or “mix” for mixtools.}

\item{general_method}{Character; “gmm”, “flex”, “mean”, or “both” (default).}

\item{post.thresh}{Numeric; posterior probability threshold (default 0.5).}

\item{prom_tol}{Numeric; minimum component proportion (default 0.05).}

\item{flex_tail}{Numeric; quantile for flex fallback (default 0.75).}

\item{bw_adjust}{Numeric; bandwidth adjustment for density (default 2).}

\item{n_grid}{Integer; grid length for density (default 512).}

\item{curvature_eps}{Numeric; curvature tolerance for flex‐fallback (default 0.01).}

\item{return.model}{Logical; if TRUE, include fitted model in output (default TRUE).}
}
\value{
A list with:
\itemize{
\item cut: numeric cutpoint
\item method: character tag of method used
\item model: the fitted EM object (if return.model = TRUE)
}
}
\description{
Fits a two-component Gaussian mixture model (via mclust or mixtools) or
applies a curvature (“flex”) fallback to estimate a data‐driven cutpoint.
}
\examples{
\dontrun{
  res <- fcs_get_em_cutpoint(x = df$FL1, em_method = "mclust")
}

}
\seealso{
mclust::Mclust, mixtools::normalmixEM
}
